name: Fake Bank API Pipeline

on: [push]

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Hello World example by using a Docker Hub image
      uses: docker://alpine
      with:
        args: echo "Hello World"
